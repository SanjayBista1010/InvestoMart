# chatbot/models.py
from django.db import models

# Note: Enterprise models for Product and ChatMessage are now located in data/models.py
# This file is kept empty to avoid conflicts with the 'products' database table.
